#include <stdio.h>

int main () {
    printf("========= Menor número primo que é maior ou igual a n =========\n");
    int n, i;
    printf("Insira um número n\n");
    scanf("%d", &n);

    for (i = n; i % n == 0; i++) {
        if (i % n == 0) {
            if ()
        }
    }
}